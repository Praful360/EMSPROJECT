@{ 
    string CurrentName = this.ViewContext.RouteData.Values["controller"].ToString();
    switch (CurrentName)
    {
        case "Admin":
            Layout = "_AdminLayout";
            break;
        default:
            //Admin layout  
            Layout = "_Layout";
            break;
    }
}  